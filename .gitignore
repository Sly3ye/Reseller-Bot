.env
.env.*
!.env.example

.venv/
.venv-verify/
venv/
__pycache__/
*.py[cod]

# Immagini scaricate (storage locale) e backup del DB
media/
backups/
# Dump e archivi media anche fuori da backups/ (non devono mai finire in git)
*.dump
media_pc_*.tar.gz

.pytest_cache/
.mypy_cache/
.ruff_cache/
*.log

playwright-report/
test-results/

# Design handoff bundles from Claude Design (source artifacts, not code)
*-handoff.zip
design_handoff/
